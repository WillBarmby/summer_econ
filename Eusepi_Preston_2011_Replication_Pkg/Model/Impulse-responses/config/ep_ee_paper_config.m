function config = ep_ee_paper_config()
%% EP_EE_PAPER_CONFIG Reproducible simulation settings for paper equation (17).
% Calibration and sample construction match the reported EE experiment;
% ep_ee_learning_config("paper",gain) separately defines what agents learn.

config=ep_ee_archive_config();
end
