# Archived Local Results

This directory contains generated outputs that no supported runner on `main`
currently produces. They are retained for local provenance and are not active
evidence. Each dated archive contains its own manifest and restoration notes.
