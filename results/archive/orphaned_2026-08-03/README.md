# Orphaned local results archived 2026-08-03

These generated outputs were moved out of the active `results/` namespace
because their producing runner is absent from `main` or the workflow has been
superseded.

- `ep_ih_initialization_robustness/` - superseded by the unified
  `initialization_robustness/` experiment.
- `ep_initialization_sensitivity/` - superseded by the unified
  `initialization_robustness/` experiment.
- `nk_initialization_sensitivity/` - superseded by the unified
  `initialization_robustness/` experiment.
- `technology_learning_mechanisms/` - generated by an experiment retained on
  branch `codex/learning-mechanism-decomposition`, not by a runner on `main`.
- `tex_comparison/` - manual TeX comparison build products, not a supported
  experiment.

To restore one folder, move it back to `results/<folder-name>/`. The archive is
ignored by Git under the repository's existing `results/.gitignore` policy.
